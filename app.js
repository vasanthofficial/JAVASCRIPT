let init = prompt("ENTER THE COMMENT");
const tasks = ["COLT", "LOOPS"];
console.log(tasks);
let newList;
while (init !== "quit" && init !== "q") {
  init = prompt("ENTER THE COMMENT");
  if (init === "list") {
    console.log("**********");
  }
}
